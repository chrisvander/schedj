[package]
name = "provider-caldav"
version = "0.1.0"
edition = "2024"

[dependencies]
calendar-provider = { path = "../calendar-provider" }
chrono = "0.4.45"
reqwest = { version = "0.12.24", default-features = false, features = ["rustls-tls"] }
thiserror = "2.0.18"
